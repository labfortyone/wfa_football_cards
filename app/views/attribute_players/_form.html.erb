<%= form_for(@attribute_player) do |f| %>
  <% if @attribute_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute_player.errors.count, "error") %> prohibited this attribute_player from being saved:</h2>

      <ul>
      <% @attribute_player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attribute_id %><br>
    <%= f.number_field :attribute_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
