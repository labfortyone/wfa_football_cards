<%= form_for(@type_player) do |f| %>
  <% if @type_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_player.errors.count, "error") %> prohibited this type_player from being saved:</h2>

      <ul>
      <% @type_player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
