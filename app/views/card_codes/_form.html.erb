<%= form_for(@card_code) do |f| %>
  <% if @card_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_code.errors.count, "error") %> prohibited this card_code from being saved:</h2>

      <ul>
      <% @card_code.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br>
    <%= collection_select(:card_code, :card_id, @cards, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label "how many codes?" %><br>
    <%= text_field :number, :times %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
